# The browser calls this same-origin path. Vite forwards it to the local backend.
VITE_API_BASE_URL=/backend
# For local development, start KOP.Api with --no-launch-profile --urls http://localhost:5121.
VITE_API_PROXY_TARGET=http://localhost:5121
